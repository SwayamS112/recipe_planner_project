# Recipe Planner - MERN (skeleton)

Front-end: React + Tailwind + ShadCN (basic)
Back-end: Node/Express/MongoDB/Cloudanery
Auth: JWT 

Run backend:
cd backend
npm install
npm run dev

Run frontend:
cd frontend
npm install
npm run dev
